#!/bin/bash
source "$(dirname "$0")/_manage.sh"

DJANGO_SETTINGS_MODULE="conf.settings"
(cd $MANAGE_DIR && \
    coverage run manage.py test && \
    coverage xml --include="$SITE_URL*" --omit="admin.py,*.virtualenvs/$PROJECT_NAME*" && \
    mv -f coverage.xml $BASE)
